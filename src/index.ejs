<!DOCTYPE html>
<html>

<head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
        <%= htmlWebpackPlugin.options.title %>
    </title>
    <% if (htmlWebpackPlugin.options.description) { %>
        <meta name="description" content="<%= htmlWebpackPlugin.options.description %>" />
    <% } %>
    <script>
        dv1testData = {
            message: 'hello from global JS object!'
        };

        dv2testData = new Promise(resolve => {
            resolve({message: 'hello from global JS promise!'})
        });
    </script>

</head>

<body>
    <div id="app1" dv-section dv-data="dv1testData">
    --> {{ message }} 123

    </div>
    <div id="app2" dv-section dv-data="dv2testData">
    --> {{ message }} 456

    </div>


    <!--div dv-root dv-id="dv1test" dv-data="dv1testData">
        {{ message }}
    </div-->
</body>

</html>